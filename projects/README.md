# Projects

Multi-session work lives here as `projects/<domain>/<YYYY-MM-DD>_<slug>/` with a README
carrying status frontmatter (active / blocked / monitor / completed). Run `/project` (no args)
to render the live priority-sorted index, or `/project <slug>` to route into one. Starts empty.
